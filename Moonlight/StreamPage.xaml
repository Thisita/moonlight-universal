<Page
    x:Class="Moonlight.StreamPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Moonlight"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <MediaElement Source="{x:Bind ViewModel.GameSession.StreamUrl, Mode=OneWay}"
                      Grid.Row="0" />
        <StackPanel Visibility="{x:Bind ViewModel.IsConnecting, Mode=OneWay}"
            Grid.Row="1"
            Width="Auto"
            HorizontalAlignment="Center"
            Padding="10,10,10,10">
            <TextBlock Text="{x:Bind ViewModel.ConnectionStatus}" />
            <ProgressBar IsIndeterminate="True" />
        </StackPanel>
    </Grid>
</Page>
